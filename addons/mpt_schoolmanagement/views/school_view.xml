<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="school_management_form" model="ir.ui.view">
        <field name="name">school.view.form</field>
        <field name="model">school.model</field>
        <field name="arch" type="xml">
            <form string="School">
                <header>
                    <field name="status" widget="statusbar" statusbar_visible="old,new,current"/>
                </header>
                <sheet>
                    <group>
                    <group>
                        <field name="name"/>
                        <field name="photo" widget="image" class="oe_avatar"/> 
                        <field name="father_name"/>
                        <field name="mother_name"/>
                        <field name="age"/>
                        <field name="role"/>
                    </group>
                    <group>
                        <field name="state"/>
                        <field name="gender"/>
                        <field name="school_dob"/>  
                        <field name="school_blood_group"/> 
                        <field name="email"/>
                        <field name="ph_no"/>
                        <field name="address"/>
                    </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="school_management_view_tree" model="ir.ui.view">
        <field name="name">school.tree</field>
        <field name="model">school.model</field>
        <field name="arch" type="xml">
            <tree string="">
                <field name="name" optional="show"></field>
                <field name="photo" optional="show" widget="image"></field>
                <field name="father_name" optional="hide"/>
                <field name="mother_name" optional="hide"/>
                <field name="age" optional="hide"></field>
                <field name="role" optional="show"></field>
                <field name="state" optional="show"></field>
                <field name="gender" optional="hide"></field>
                <field name="school_dob" optional="show"></field>
                <field name="school_blood_group" optional="hide"></field>
                <field name="email" optional="show"/>
                <field name="ph_no" optional="hide"/>
                <field name="address" optional="show"/>
            </tree>
        </field>
    </record> 

   
    <record id="action_school_management" model="ir.actions.act_window">
        <field name="name">School Management</field>
        <field name="res_model">school.model</field>
        <field name="view_mode">tree,form</field>
    </record>


    <menuitem
        id="school_management_root_menu"
        name="School Management"
        sequence="10"/>
        <menuitem
            id="school_management_menu"
            name="Campus"
            action="action_school_management"
            parent="school_management_root_menu"
            sequence="10"/>

</odoo>
